import React from 'react';

const Landing = () => {
  return (
    <div>
      <form>
        <input type="email" name="email" />
        <button>login</button>
      </form>
    </div>
  );
};

export default Landing;
